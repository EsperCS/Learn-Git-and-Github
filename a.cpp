#include <bits/stdc++.h>
using namespace std;

int main() {
    if (fopen("input.inp", "r")) {
        freopen("input.inp", "r", stdin);
        freopen("output.out", "w", stdout);
    }
    cin.tie(0)->sync_with_stdio(0);

    cout << "Hello World";

    return 0;
}